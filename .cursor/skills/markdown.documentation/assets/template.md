# {ドキュメント名}

## 概要

{対象コンポーネントの目的と役割を簡潔に説明する。}

* {主要な特徴または設計思想}
* {関連コンポーネントとの関係。不要な場合は削除する。}

<!--
以下の「原則・規則」セクションは、扱う原則・規則ごとに複製する。
見出しは文書内で一意にし、実際の名称へ置き換える。
-->

## {原則・規則名}

{守るべき原則・規則を簡潔かつ明確に記述する。}

### {原則・規則名}の補足

{原則・規則の意図や背景を説明する。}

* 利点: {この原則・規則を適用する利点}
* 注意点: {適用時の注意点}
* 適用範囲: {適用対象}
* 例外: {例外。存在しない場合は「なし」と記述する。}

### {原則・規則名}の実装例

{実装例が原則・規則をどのように満たすか簡潔に説明する。}

```dart
// {パッケージ名}, {実在するファイル名}.dart
{実際のコードベースから引用した、必要最小限のコード}
```

<!--
悪い例が必要な場合のみ残す。
架空のコードではなく、実際のコードまたは汎用的な提案コードを用いる。
-->

#### {原則・規則名}で避けるべき実装

{この実装を避けるべき理由を説明する。}

```dart
// {パッケージ名}, {実在するファイル名}.dart
{避けるべき実際のコード。省略部分はコメントで示す。}
```

<!--
必要な場合のみ「実装詳細」セクションを残す。
ディレクトリ構成は事前に実在を確認し、プロジェクトルートからの相対パスで記述する。
-->

## ナレッジベース

<!-- 
* 二重否定による混乱の防止するよう、テキストは`DO`で揃える

例: DIの利用を推奨する場合
DO: Dependency Injectionを使用する

例: static変数の利用を防止したい場合
DO NOT: static変数を使用する
 -->

### DO: {1行で内容を端的に表す}

* 推奨パターンのexample等

```dart
// 推奨パターンのexample
```

### DO NOT: {1行で内容を端的に表す, 二重否定にならないようにすること}

* 理由: {DO NOTである理由を箇条書きする}

```dart
// 非推奨パターン
// DO NOT: XXXXX

```

```dart
// 推奨される書き換えパターン
// DO: YYYY
```

## 参考リンク

* {リンクの説明}: <{URL}>
